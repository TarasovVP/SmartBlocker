<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="@dimen/padding_16"
        android:paddingTop="@dimen/padding_8">

        <TextView
            android:id="@+id/filter_list_filter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="@dimen/padding_5"
            android:text="@string/filter_no_filter"
            android:visibility="invisible"
            app:drawableStartCompat="@drawable/ic_filter_list"
            android:drawablePadding="@dimen/padding_10"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/filter_list_refresh"
            android:layout_width="match_parent"
            android:layout_height="@dimen/size_0"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/filter_list_filter">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/filter_list_recycler_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scrollbars="vertical" />
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <include
            android:id="@+id/filter_list_empty"
            layout="@layout/include_empty_state"
            android:layout_width="match_parent"
            android:layout_height="@dimen/size_0"
            app:layout_constraintTop_toBottomOf="@+id/filter_list_filter"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <com.tarasovvp.blacklister.utils.FloatingActionMenuView
            android:id="@+id/filter_list_fab_menu"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
